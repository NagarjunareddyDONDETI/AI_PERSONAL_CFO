# Core web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
pydantic==2.10.3

# Data / ML
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2

# Multi-format statement ingestion
openpyxl==3.1.5
xlrd==2.0.2
pdfplumber==0.11.10
chardet==7.4.3

# Environment
python-dotenv==1.0.1

# RAG (Phase 3/4)
# chromadb 1.x ships prebuilt abi3 wheels and no longer needs chroma-hnswlib.
# Do NOT pin back to 0.5.x: that line depends on chroma-hnswlib, which has no
# wheel for Python 3.13 and fails to build without a C++ toolchain.
chromadb==1.5.9
# sentence-transformers is OPTIONAL. Chroma bundles an ONNX build of the same
# all-MiniLM-L6-v2 model (384 dims) that runs on onnxruntime, so embeddings work
# without it. Install it only to use a different RAG_EMBED_MODEL — it pulls torch
# (several GB):
#   pip install sentence-transformers
langchain==0.3.9
langchain-core==0.3.21
langgraph==0.2.53

# Multi-provider LLM layer (async HTTP transport for all providers)
httpx==0.28.1
# Optional cache backend — only needed if REDIS_URL is set:
#   pip install redis
# redis==5.2.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
respx==0.22.0

# Voice (Phase 6)
# gtts pins click<8.2 while chromadb's typer wants a newer click. pip reports the
# conflict, but gtts only uses click for its CLI, so the Python API is unaffected.
gtts==2.5.4
# openai-whisper is heavy (pulls torch). Install manually if needed:
#   pip install openai-whisper
